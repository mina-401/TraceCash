services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root-password
      MYSQL_DATABASE: trace-cash
      MYSQL_USER: mina
      MYSQL_PASSWORD: mina-password
    ports:
      - "3307:3307"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: